const emailController = require('../controllers/emailController');

const emailRoute = (app) => {
    app.post('/email', (req, res, next) => {
        //TODO: Add call in here
    });
}

exports.emailRoute = emailRoute;
